const progressPercentageOptions = {
    cutout: "70%", // Adjusts the size of the inner hole for the donut chart
    responsive: true,
    maintainAspectRatio: false,
  };

module.exports = {progressPercentageOptions}